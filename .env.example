# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key_here

# Note: MCP servers are now configured via the web interface at /mcp_servers
# When no servers are configured, chat works as a regular AI assistant without tools